# LW Lab 网站维护规范

## 内容来源

- **网站当前内容唯一来源：website-content.json**。成员顺序、导师简历、新闻、联系方式和论文都在其中；本文件不再重复维护名单和正文。
- 编辑后运行 `python tools/sync_content.py` 同步到两个 HTML 页面；缓存版本自动更新。
- 新增事实应来自实验室确认、提供的简历或可核对来源，不编造时间、经费和任职。简历原件不放入网站仓库。
- 实验室英文全称为 Limitless Wonder Laboratory，首页徽标文字为 Limitless Wonder Lab。不要在网站解释 L 与 W 的姓名来源。
- 成员只有身份标签，不标年级。照片缺失时沿用姓氏占位，不随意增删成员。分组顺序：博士 → 科研工作人员(Staff) → 硕士 → 本科生。

## 视觉与布局

- 白底、留白、深灰正文，深蓝用于链接和小标题；整体保持简洁学术风。
- 固定顶栏、首页分区锚点导航，论文为独立 publication.html。
- 系统字体栈，图片与脚本本地存放，不使用外部 CDN。
- 两位导师各一张完整简介卡，王茜在左、刘立鸿在右；手机依次上下排列，王茜在前。合并重复头像与履历，保留有用信息。
- 经用户 2026-09-14 指定，导师卡允许彩色动效：王茜粉色、刘立鸿蓝色；鼠标进入整张卡片后背景平滑变化，不使用跟随鼠标的局部光晕。键盘聚焦有可见反馈，减少动效偏好下禁用动画。
- Hero 的图形粒子效果保持；徽标上两行文字独立渲染为清晰文字，支持缓慢随机变色、鼠标水波与字母轻抖。不得重新把文字做成粒子。
- 合影保留自动与手动轮播；最新论文多于一张时轮播。
- 新闻条目支持可选配图（JSON 中 image / image_alt / image_caption 字段），缩略图点击放大为灯箱，
  点任意处或 Esc 关闭（2026-09-16 新增）。

## 文件与图片

- index.html、publication.html、css/style.css、js/main.js 为直接发布的静态网站，无前端构建步骤。
- 全部文本使用 UTF-8。
- 新照片先按内容文件里的文件名放入 assets/photos/，再运行 tools/check_images.py。
- 超尺寸图片缩至横图长边 1920px、竖图/方图长边 1200px，JPEG 质量 85；小图不重编码；原图保留到 assets/originals/，避免覆盖同名原图。
- 更换徽标时注意 JS 内联了原图；图形采样区域与独立文字位置需要一起核对。

## 验证

- 内容同步检查通过；主页、论文页、导航和轮播可用。
- 桌面、768px、375px 无横向溢出；导师卡顺序正确，文字不遮挡。
- 新照片加载正常；未提供照片的成员保留占位。
- 控制台无 JS 异常；file:// 离线打开仍可用。
- 对照新增事实和变更范围检查差异，发布沿用现有 GitHub Pages 流程。
