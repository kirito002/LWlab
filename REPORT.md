# LW Lab 网站完工报告

完工日期：2026-09-11
依据文件：`SPEC.md`（唯一内容与设计依据）

## 1. 文件结构

```
lab-website/
├── SPEC.md            # 规格书（原有，未改动）
├── PROGRESS.md        # 建设进度记录
├── REPORT.md          # 本报告
├── .gitignore         # 原有，未改动
├── index.html         # 单页站点（Hero / 关于 / 研究 / 团队 / 新闻 / 加入我们 / 页脚）
├── css/style.css      # 全部样式（白底极简学术风，系统字体栈）
├── js/main.js         # 仅移动端导航开合（约 20 行，无依赖）
└── assets/photos/     # 成员照片目录（当前为空，放入照片即自动显示）
    └── README.md      # 原有，未改动
```

纯静态、零构建、零外链：所有字体走系统字体栈回退，favicon 为内联 data-URI，
断网、`file://` 双击打开均完整可用。

## 2. 如何打开

任选其一：

- **双击 `index.html`**（file:// 协议即可，功能完整）。
- 或在本目录运行 `python -m http.server 8000`，浏览器访问 `http://127.0.0.1:8000`。

## 3. 自检结论（对照 SPEC §6 验收标准）

| 验收项 | 结果 |
|---|---|
| 双击/静态服务器完整可浏览 | ✅ 两种方式均验证通过 |
| 锚点跳转正确、无死链 | ✅ 6 个导航锚点与分区 id 一一对应，#people/#news/#join 实测点击跳转正确 |
| 控制台无 JS 报错 | ✅ 页面仅有 main.js（菜单开合），实测功能正常无报错 |
| 22 名成员全部渲染 | ✅ 导师 2 人 + 学生 20 人，顺序与 SPEC §4 完全一致，均有照片预留位 |
| 375 / 768 / 1440px 不破版 | ✅ 三档视口截图自查，无溢出无错位；移动端汉堡菜单正常 |
| 中文无乱码、白底学术感 | ✅ 全站 UTF-8；纯白背景、衬线标题、强调色仅深蓝 #1a3a6b |

照片占位机制：每张卡片为 `<img src="assets/photos/xxx.jpg">` + `onerror` 回退，
加载失败时显示浅灰底 + 姓氏首字。**今后把真实照片按 SPEC §4 的文件名放入
`assets/photos/` 即自动显示，无需改任何代码。**

## 4. 遗留的【待补充】事项清单

以下均为 SPEC 中标记占位、需实验室成员自行补充的内容，不影响站点可用性：

- **Hero/关于**：依托单位信息（SPEC 标注【依托单位待补充】）。
- **研究**：4 个研究方向卡片的真实名称与描述（当前为通用占位描述）。
- **团队**：两位导师的职称（【职称待补充】）。
- **新闻**：当前 2 条为示例（【示例，待替换】），可直接编辑 `index.html` 中新闻区块替换。
- **加入我们**：联系邮箱、通信地址（当前为【待补充】；补充后请同时把
  `mailto:【待补充】` 改为真实邮箱）。
- **照片**：22 张成员照片按 SPEC §4 文件名放入 `assets/photos/`。
- **拼音**：生僻字读音（翀/瑗/璟/焱/玙璠等）如与本人习惯不符，请成员直接修改 SPEC.md。

## 5. 未做的事项

- **GitHub Pages 发布：已跳过。** 加分项要求本机 gh CLI 已安装且已登录才可执行，
  实测本机未安装 gh（`gh: command not found`），按约定未尝试安装或登录。
  如日后需要发布，安装并登录 gh 后可用 `gh repo create` + `gh pages` 或手动推送
  本目录至 GitHub 仓库并开启 Pages（纯静态站可直接以 main 分支根目录发布）。
- 未注册任何账号、未购买任何服务、未输入任何凭据。
- 未修改 lab-website 目录以外的任何文件；未删除目录内已有文件。
